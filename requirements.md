# グラフ・バトルロイヤル ゲーム要件定義書

> **対象読者**: 実装担当（Claude Code）
> **技術スタック**: React + Vite + TypeScript
> **最終目的**: 本ゲームを**強化学習（RL）エージェントの学習環境**として使用し、学習済みポリシーをプレイヤーとして動かす
> **この文書の使い方**: 「2. 確定仕様」までを実装対象とする。「12. ゲーム性向上の提案」は**採用未確定**。実装前に発注者へ採否を確認すること。数値はすべて `config` に外出しし、既定値は調整前提とする。

---

## 1. アーキテクチャ方針（最重要）

本作は「遊べるゲーム」であると同時に「RLの学習環境」である。この両立のため、**シミュレーション本体を描画・React・DOMから完全に分離**すること。これを守らないと学習速度が出ず、目的を達成できない。

### 1.1 モジュール分割

| ディレクトリ | 責務 | 依存 |
|---|---|---|
| `src/sim/` | 純粋なゲームロジック。マップ生成・エンティティ・tickループ・移動・戦闘・テリトリー・リング。**DOM/React/描画に一切依存しない** | なし（seed付きPRNGのみ） |
| `src/env/` | RL環境ラッパー。観測・行動・報酬のエンコード。Gym/PettingZoo風API（`reset`/`step`） | `sim` |
| `src/render/` | 神視点の描画（Canvas/WebGL）。`sim`の状態スナップショットを購読して描くだけ。ロジックを持たない | `sim`（読み取りのみ） |
| `src/agents/` | ポリシーインターフェース。スクリプトbot＋学習済みポリシーローダ | `env` |
| `src/app/` | React UI（再生/停止/ステップ実行/速度/seed入力/デバッグ表示） | `render`, `sim`, `agents` |

### 1.2 満たすべき非機能要件

- **ヘッドレス実行**: `sim`＋`env`は Node.js 上で描画なしに動くこと。学習ループはブラウザを介さない。
- **決定性**: 同一 `seed` ＋同一行動列 → 完全に同一の結果。`Math.random()` 禁止。乱数は単一のseed付きPRNG（例: `mulberry32` / `xoshiro128**`）に集約。
- **状態のシリアライズ**: ゲーム全状態を JSON（または構造化オブジェクト）へ入出力可能にする。リプレイ・スナップショット・後述のPythonブリッジに必須。
- **高速性**: 描画なしで多数の対戦を並列に回せる設計。1 tick の計算は割り当てベースの二相更新（後述）で O(ユニット数 + 近傍ノード数) に収める。
- **ベクトル化**: 複数環境インスタンスを同一プロセスで並列生成できること。

---

## 2. 座標系とマップ

### 2.1 座標系

- 「正三角形を敷き詰めた頂点群」＝**三角格子**。各頂点は最大6近傍を持つ。これは**六角格子（hex grid）と同じ座標数学**で扱える。
- 内部座標は **cube座標 `(x, y, z)`（`x+y+z=0`）** もしくは等価な **axial `(q, r)`** を採用。
  - ホップ距離（隣接段数）: `hexDist = (|dx| + |dy| + |dz|) / 2`
  - 6方向の隣接オフセットを定数配列で保持。
- **2D埋め込み**: 描画と「辺上の連続位置」計算のため、各ノードを平面座標へ写像する。基底ベクトル `e0=(1, 0)`, `e1=(0.5, √3/2)`、`world(q,r) = q·e0 + r·e1`。**辺長 = 1.0**。
  - 辺上の位置は端点2ノードの world 座標の線形補間で表す（後述の `progress`）。

### 2.2 マップ生成

1. **半径 `R`（既定 25、config）** のcube座標で全ノードを生成。ノード総数 ≈ `1 + 3R(R+1)`（R=25 で約1951）。
2. 各ノードの **標高 `elevation ∈ [0,1]`** をパーリンノイズで決定（world座標を入力、seed依存、周波数はconfig）。
3. **壁判定**: 標高が **固定閾値 `wallThreshold`（config）未満**のノードを `passable=false`（通行不可・壁）とする。閾値は壁が全体の約30%になる値を基準に設定するが、**seedによって壁の割合・配置が大きく変わることは許容する**（パーセンタイルによる動的調整はしない）。
4. **連結性保証**: 通行可能ノードの**最大連結成分**のみを採用し、それ以外の孤立小島は壁化する（または壁を削って接続）。これにより経路探索が常に成立する。
5. **エッジ生成**: 通行可能ノード同士が隣接する場合のみ辺を張る（壁へは張らない）。

> **RL上の注意**: R=25 で総ノード約1950・通行可能は約1400。学習初期はさらに小さいR（例 10〜15）から始め、段階的にR=25へ拡大するカリキュラムを推奨（詳細は 11.6）。

---

## 3. エンティティ

### 3.1 チーム / ユニット

- **チーム数**: 最大8（config、`2〜8`）。
- **1チーム = 3ユニット**。各ユニットは独立に行動。**ユニット間の性能差なし**。
- スポーン: 各チームを互いに離れた通行可能ノードへ配置（マップ外周付近に均等分散、seed依存）。同一チームの3体は近接スポーン。

### 3.2 ユニットの状態フィールド（sim内部）

| フィールド | 型 | 説明 |
|---|---|---|
| `id`, `teamId` | number | 決定性のための一意ID（更新順序に使用） |
| `pos` | `{node}` or `{from, to, progress}` | ノード上、または辺上（progress∈[0,1]）の連続位置 |
| `hp` | number | 体力（既定100） |
| `alive` | bool | 生存フラグ |
| `command` | enum + payload | 現在の移動指令（方向/目的地/待機）、攻撃対象 |
| `destination` | node? | 目的地（あれば） |

### 3.3 ノードの状態

| フィールド | 型 | 説明 |
|---|---|---|
| `elevation` | number | [0,1] |
| `passable` | bool | 壁でないか |
| `owner` | teamId? | テリトリー所有チーム（null=中立） |
| `captureProgress` | `{teamId, ticks}?` | 敵エリア奪取の進行状況 |

---

## 4. ゲームループ（tick）

### 4.1 時間の粒度

- **tick**: シミュレーションの原子ステップ。移動・戦闘・テリトリー・リングはtick単位で進む。
- **意思決定間隔 `D`（既定5、config）**: RLエージェントは `D` tick ごとに新しい行動を出す（フレームスキップ）。間の tick は直近の指令を継続実行。

### 4.2 二相更新（決定性のため必須）

各 tick で以下を厳守：

1. **Readフェーズ**: 全ユニットの意図（移動先・攻撃対象）を、更新前の状態から計算。
2. **Applyフェーズ**: `(teamId, unitId)` の昇順で確定的に適用（移動→戦闘→テリトリー→リング判定→死亡判定の順）。同時発生の競合はID昇順でタイブレーク。

これにより更新順依存の非決定性を排除する。

---

## 5. 移動

- ユニットは**辺に沿って一定速度**で進む。**基本速度 `v`（既定 0.1 world単位/tick ＝ 10 tick/辺、config）**。
- **辺上位置**は `{from, to, progress}` で表現。`progress` が 1.0 に達したら `to` ノードへ到達（テリトリー・リング判定を発火）。
- **目的地とパス**: 指令が「目的地ノード」の場合、通行可能ノード上でA*等により経路を求め、次辺へ進む。目的地未設定時の既定挙動＝未探索方向へ探索（下記）。
- **自エリア移動ボーナス**: 進行中の辺の**両端が自チームのエリア**なら速度 `×(1 + territoryMoveBonus)`（既定 +20%）。
- **辺の途中での指令変更**: 許可。現在の辺を反転（`from`/`to` 入れ替え、progress反転）または継続。逆走・目的地変更を認める。
- **経路消失時**（壁化やリングで到達不能）: 再探索。到達不能なら待機。

> **RLとの関係**: 上記「探索して目的地を設定」は**エージェント未介入時のフォールバック（スクリプト挙動）**として実装する。RLポリシーが行動を出した tick 以降はポリシー指令が優先（行動空間は 11.3）。

---

## 6. テリトリー（塗り）

- **中立ノード**: ユニットが到達 or 通過した瞬間に**即座に自チームのエリア化**（`owner = teamId`）。
- **敵ノード**: そのノードに**`captureTicks`（既定30、config）滞在**することで自チーム化。通過（停止せず抜ける）だけでは敵ノードは奪えない。
  - 進行中に自ユニットがそのノードを離れたら `captureProgress` はリセット。
  - 同一ノードに敵味方双方が存在する「係争状態」では進行を凍結（or リセット、config）。
- **自エリアの効果**:
  - 攻撃力ボーナス `×(1 + territoryAtkBonus)`（既定 +10%、攻撃側が自エリアノード上にいるとき）。
  - 移動ボーナス（5節）。
- **永続性**: 一度塗ったエリアはユニットが離れても保持（既定では減衰なし。減衰は 12節の提案）。

---

## 7. 戦闘

### 7.1 視認と射程

- **視認距離 `visionRange`（既定 6.0 world単位、config）**: この距離内の敵ユニットを「視認」できる。基本仕様では地形遮蔽なし（LoS遮蔽は 12節の提案）。
- **攻撃射程 `attackRange`（既定 2.0 world単位、config）**: 「半径2ノード以内」を world 距離2.0として実装。**攻撃側・対象がノード上か辺上かを問わず**、両者の world 座標間ユークリッド距離 ≤ `attackRange` で命中判定。
- 攻撃成立条件 = 対象が**視認可能**かつ**射程内**かつ**敵チーム**。味方への攻撃は不可。

### 7.2 ダメージ

- **単一対象**: 各ユニットは1 tickにつき攻撃対象を高々1体。
- 攻撃成立tickごとに継続的にダメージ:

```
damage_per_tick = baseDamage
                × (1 + territoryAtkBonus if 攻撃側が自エリア上)
                × highGroundCoef
```

- **高所係数**:
```
highGroundCoef = clamp(1 + k × (elev_attacker − elev_target), coefMin, coefMax)
既定: k=1.0, coefMin=0.7, coefMax=1.5
```
（辺上にいる場合は端点標高の補間値を使用）
- **baseDamage 既定 1.0 HP/tick**（補正なし1v1で約100 tickでフルキル。調整前提）。
- **脱落**: `hp ≤ 0` でそのユニットは `alive=false`。以後、行動・テリスト・視認対象から除外。同一tickでの相打ちは両者脱落を許容。

### 7.3 攻撃対象の選択

- どの敵を撃つかは行動空間で選ぶ（11.3）。射程外/非視認になった tick はその攻撃は不発。

---

## 8. リング（安全地帯の収縮）

- **段階 `ringStages`（既定6、config）**。各段階は **警告フェーズ（`warnTicks`、既定120）→ 収縮フェーズ（`shrinkTicks`、既定180）** からなる。収縮中は安全半径を現半径→次半径へ線形補間。
- **中心**: 段階ごとに**現在の安全圏内の通行可能ノードから新中心を抽選**（seed依存、到達可能性を担保）。中心は毎回変わる。壁ノードは中心にしない。
- **半径スケジュール（world単位、例・config）**: `R_map → 18 → 13 → 9 → 5 → 2 → 0`。最終段は**単一ノード**（必ず通行可能ノード）に収束。
- **圏外**: 移動は可能だが **スリップダメージ**（`slipDamage`、既定は段階が進むほど増加。例 0.3→0.5→0.8→1.2→…HP/tick、config）。
- **情報開示**: 各ユニットには **「次に収縮するリングの中心・半径」だけ**を与える（それ以降の未来リングは非開示）。→ 観測設計（11.2）に反映。

---

## 9. 勝敗条件

- **勝利**: 生存ユニットを持つ最後の1チーム。
- **最終リング（単一ノード）到達後も複数チーム生存**の場合: 圏外スリップが全域に及ぶため戦闘で自然決着。
- **タイブレーク**（同一tick全滅など）: ①総HP大 → ②保有テリトリー数大 → ③seed由来の乱数、の順。
- **順位**: 脱落時刻の遅い順にチームを順位付け（報酬設計に使用）。

---

## 10. エッジケース・決定性ルール（実装時に必ず対応）

- ユニット更新順は `(teamId, unitId)` 昇順で固定。
- 全乱数は単一seed付きPRNG。マップ生成・スポーン・リング中心すべて同一系列から派生（サブストリームで分離可）。
- 同一敵ノードを2チームが同時奪取 → 係争凍結（6節）。
- ユニットが辺の途中で脱落 → 除去し、進行中の奪取は中断。
- リング中心が壁に当たる → 最近傍の通行可能ノードへ補正。最終ノードは必ず通行可能かつ到達可能。
- 攻撃対象が tick 内に射程/視認を外れる → その tick は不発。
- 目的地が到達不能化 → 再探索、不能なら待機。

---

## 11. 強化学習インターフェース（`src/env/`）

### 11.1 マルチエージェント構造（要方針決定）

- **推奨**: **分散・重み共有ポリシー（1ポリシーが1ユニットを制御、全ユニットでパラメータ共有）＋自己対戦（self-play）**。対称ゲームと相性が良く、スケールしやすい。学習は中央集権・実行は分散（CTDE）を任意で併用可。
- **代替案**: チーム単位コントローラ（1ポリシーが3ユニット分の行動をまとめて出力）。協調は取りやすいが行動空間が大きくなる。
- → まず推奨案で実装し、必要なら代替案に切替可能な抽象化にしておく。

### 11.2 観測空間（1ユニット・エゴセントリック・固定長）

**全マップ（〜7600ノード）をそのまま入力しないこと。** 以下の固定長表現にする：

- **自身**: リング中心への相対位置、リング半径・収縮までの残tick、圏内フラグ、標高、HP、移動状態（ノード上/辺上・progress・向き）、足元の所有者（自/敵/中立）。
- **味方（2体）**: 相対位置・HP・生存フラグ。
- **視認中の敵（上限 `N`、既定6）**: 相対位置・HP・標高差・射程内フラグ。近い順、パディング＆マスク。
- **局所地形パッチ**: 自身中心の **半径 `k` ホップ（既定3〜4）の六角パッチ**を多チャンネル・グリッドで。チャンネル例＝ `elevation` / `wall` / `owner(自/敵/中立の3ch)`。→ 全体グラフ不要で空間把握を与える。
- **全体サマリ**: 各チームの生存ユニット数、自チームのテリトリー占有率、経過tick・現段階。

すべて正規化して連結。**部分観測**（視認外の敵は見えない・未来リングは見えない）を前提とする。

### 11.3 行動空間（1ユニット・離散・`D` tickごと）

`MultiDiscrete([7, N+1])` を推奨：

- **移動ヘッド（7）**: `{待機, 6方向のいずれかへ一歩進む}`。壁方向は行動マスクで無効化。
  - 低レベル方向指令＝行動空間が固定小サイズで学習しやすい。目的地全ノード選択（〜7600）は不可（爆発する）。
  - *代替（階層型）*: `{リング中心へ / 最寄り敵へ / 最寄り未占有へ / 待機 / 退避}` のオプション。表現力は落ちるが探索が速い。両立させるなら低レベルを既定とする。
- **攻撃ヘッド（N+1）**: `{攻撃しない, 視認中の敵スロット0..N-1}`。射程外・非視認・非生存はマスク。
  - *簡易版*: 「射程内の最寄り敵を自動攻撃」にして攻撃ヘッドを廃止も可（初期プロトタイプ向け）。

**行動マスク**（無効手の除外）を `step` の `info` で必ず提供すること。

### 11.4 報酬シグナル（生の素材・整形は要注意）

報酬ハッキングを避けるため、以下を**係数付きで合成**し、係数はconfig化。既定は自己対戦で調整。

- 終端: 順位ボーナス（勝利＝大、順位で逓減）。
- 戦闘: 与ダメージ `+`、被ダメージ `−`、敵ユニット撃破 `+`、自ユニット喪失 `−`。
- テリトリー: 新規占有ノード `+`（過剰だと塗り固執を誘発するので小さめ）。
- 生存: 生存tick 微小 `+`（or 消極化を避けるため微小 `−`）。
- リング: スリップ/圏外に `−`。

> **注意**: 固定相手への過学習を避けるため、**自己対戦＋過去バージョンのプール（league）**を推奨。報酬は最初は疎な順位報酬中心にし、必要な分だけ整形を足す。

### 11.5 環境API（Gym / PettingZoo風）

- `reset(seed) -> obs（エージェントごと）`
- `step(actions) -> (obs, rewards, terminations, truncations, infos)`
- エージェント集合 = 生存ユニット。**途中脱落を扱える**こと（PettingZooのParallel/AECいずれか）。
- ヘッドレス・決定性・状態シリアライズ対応（1節）。ベクトル化対応。

### 11.6 学習ツールの選択肢（要方針決定）

| 案 | 内容 | 長所 | 短所 |
|---|---|---|---|
| **A（プロト向け）** | Node上で **TensorFlow.js** により純TSで学習 | 単一言語・同一simをそのまま使用 | 速度・RLエコシステムが弱い |
| **B（本格学習向け・推奨）** | simをNodeヘッドレスで動かし、**stdio/msgpack等でPython（Gymnasium/PettingZoo＋RLlib/SB3/CleanRL）へブリッジ** | PPO・league等の実績ツール・ベクトル化で高速 | 疎結合APIの整備が必要 |
| **C（非推奨）** | simをPythonへ再実装 | Python完結 | 実装二重化でロジック乖離リスク |

- **推奨**: simを**純粋・シリアライズ可能・依存最小のTSモジュール**として作れば A/B 双方に対応可能。Aで素早く回し、規模が必要になったらBへ。
- **推論の逆輸入**: 学習済みポリシーを **TF.js形式でエクスポート**し、React上（`src/agents/`）でライブ実行 → ブラウザでRLボットと対戦・観戦可能にする。
- **カリキュラム**: `R` 小 → 大、チーム数少 → 多、の順で難度を上げる。

---

## 12. 描画要件（`src/render/`）

- **神視点（トップダウン）**。ノード約7600＋ユニット＋テリトリーを描くため、**Canvas/WebGL（PixiJS / regl 等）を推奨**。SVG/DOMは規模的に不可。
- 描画要素: 通行可能ノード（点/六角）、壁（別色）、標高（グレースケール or 段彩）、テリトリー（チーム色の淡い着色）、ユニット（チーム色マーカー＋HPバー）、辺（任意）、リング（境界線＋圏外の危険色）、攻撃線（攻撃者→対象）。
- **sim/renderの分離**: simは状態スナップショットを発行、renderは読むだけ。
- **デバッグUI**（RL開発に必須）: 再生/停止/1tickステップ/速度変更/seed入力、視界・射程・リング・観測パッチの可視化オーバーレイ、seed＋行動ログからの**リプレイ再生**。

---

## 13. config（既定値一覧・すべて調整前提）

| キー | 既定 | 説明 |
|---|---|---|
| `mapRadius` | 25 | マップ半径（学習初期はより小さめ推奨） |
| `wallThreshold` | (約30%相当) | 標高の固定閾値。未満を壁とする。seedで壁割合が変動することを許容 |
| `perlinFrequency` | (調整) | 標高ノイズ周波数 |
| `teamCount` | 8 | チーム数（2〜8） |
| `unitsPerTeam` | 3 | 1チームのユニット数 |
| `unitHP` | 100 | 体力 |
| `baseSpeed` | 0.1 | world単位/tick（10 tick/辺） |
| `territoryMoveBonus` | 0.20 | 自エリア移動速度ボーナス |
| `decisionInterval D` | 5 | 意思決定間隔（tick） |
| `captureTicks` | 30 | 敵ノード奪取に要する滞在tick |
| `territoryAtkBonus` | 0.10 | 自エリア攻撃力ボーナス |
| `baseDamage` | 1.0 | 攻撃成立tickあたりダメージ |
| `highGround k / coefMin / coefMax` | 1.0 / 0.7 / 1.5 | 高所係数 |
| `visionRange` | 6.0 | 視認距離（world） |
| `attackRange` | 2.0 | 攻撃射程（world、「2ノード」） |
| `maxVisibleEnemies N` | 6 | 観測・攻撃対象の上限 |
| `patchHops k` | 3〜4 | 局所地形パッチ半径 |
| `ringStages` | 6 | リング段階数 |
| `warnTicks / shrinkTicks` | 120 / 180 | 警告/収縮フェーズ長 |
| `ringRadiusSchedule` | R→18→13→9→5→2→0 | 半径推移 |
| `slipDamage` | 段階増（例0.3〜） | 圏外スリップ |

---

## 14. 実装フェーズ（推奨順）

1. **simコア**: 座標系・マップ生成（Perlin＋壁＋連結保証）・エンティティ・tickループ・移動。ヘッドレス＆決定性のユニットテスト付き。
2. テリトリー・戦闘・リングを追加。
3. **env**（観測/行動/報酬・PettingZoo風API）。ランダム行動での自己対戦スモークテスト。
4. **描画**（WebGL神視点＋操作UI＋リプレイ）。
5. **学習パイプライン**（案A or B）、スクリプトbotのベースライン、カリキュラム（小R→大R）。
6. バランス調整（configチューニング）、league/self-play。

---

## 15. ゲーム性向上の提案（採用未確定・発注者判断）

> 以下は**任意提案**。既定仕様には含めない。採用するものだけ実装。

1. **地形による視線遮蔽（LoS）**: 高標高のノードが視線を遮る。高所の価値がダメージ倍率だけでなく「索敵・隠密」に広がり、RLで待ち伏せ等の戦術が創発しやすい。
2. **テリトリーの意味付け強化**: 占有ノード数を「回復/リソース」に変換（自エリアで非戦闘時にHP微回復など）。塗りが survival に直結し、盤面支配の動機が生まれる。
3. **補給/バフノード**: 特定ノードで一時バフ（回復・射程・視界）。マップに目的地が生まれ、支配争いの焦点になる。
4. **復帰/増援**: 一定以上のテリトリー確保で脱落ユニットを復活。スノーボール（一方的展開）を緩和し、対戦が長持ちする。
5. **チーム内視界共有**: 味方が見た敵を共有。協調MARLの学習が安定しやすい。
6. **移動中の攻撃ペナルティ**: 辺上では命中/威力を下げる。「止まって撃つ」という位置取りの判断が生まれる。
7. **高所の視界ボーナス**: 高所は視認距離も伸びる。稜線の価値が上がる。
8. **マップ地形制約**: 生成時にチョークポイント（壁の隘路）が生まれるよう連結性・隘路度を評価して再生成。単なるランダムな塊より戦術的な地形になる。
9. **非戦闘時のリジェネ / 戦闘中は不可**: 撃ち合いの間合い管理（引く・粘る）の判断を作る。
10. **スポーン時のわずかな編成選択**（性能差なし原則を崩すため任意）: 「射程寄り/移動寄り/防御寄り」等のロードアウトを選ばせると読み合いに深みが出る。対称性を崩すので慎重に。
